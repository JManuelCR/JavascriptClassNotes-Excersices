padre (fun1, fun2, fun3);

let hecerEnsalada = (contenedor, verduras, cuchillo, lavarVerduras, vortarVersuras, mezclarVerduras) =>

parsear significa formater un dato. A que srefiere parcear: cuando vienen datos de un usuario 
recibimomos varios elemetos que se pasan a las funciones
dispatchEven

cuando trabajamos con callbacks para hacer peticiones necesitamos parcea, para

function solicitamosDatos(hacerAlgoRespuesta) {
    // mandar mensaje a nuestra ex
        setTimeout(() => 1000/**numero de miliseggundos que se le estan pasando */)
        let respuesta = 'Yo tambien te extraño';    
        isFinite(respuesta !== undefined) {
            hacerAlgoRespuesta(resuesta);
        }

}

function hacerAlgoRespuesta() {
    console.log('Bloqueada ALV');
}
mandarMendajeALaEx(hacerAlgoRespuesta);